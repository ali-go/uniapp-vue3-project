<script>
export default {
	// onLoad(query) {
	// 说明
	// 暂时模拟请求产品列表数据放在首页做了，动态获取第一个产品渲染，后续如果显示不够及时，
	// 就调整到主包请求产品列表，并携带第一个产品的产品参数跳这里并重定向到首页，但是这种情况，
	// 产品列表的数据就得放在主包上了，后面看情况是否调整
	// const productId = query.productId
	// uni.redirectTo({ url: `/packageCrossBorderE/pages/index/index?productId=${productId}` })
	// },
	methods: {}
}
</script>
